import { PostTitle } from './post-title';
import { PostContent } from './post-content';

export const PostBody = () => {
    return (
        <div>
            <PostTitle />
            <PostContent />
        </div>
    );
};
